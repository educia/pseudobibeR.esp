name: Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::testthat, any::devtools
          needs: check

      - name: Download UDPipe French model for tests
        run: |
          Rscript -e "
          if (!dir.exists('tests/models')) dir.create('tests/models', recursive = TRUE)
          udpipe::udpipe_download_model(language = 'french-gsd', model_dir = 'tests/models')
          "

      - name: Run Tests
        run: |
          devtools::test(stop_on_failure = TRUE, stop_on_warning = TRUE)
        shell: Rscript {0}
